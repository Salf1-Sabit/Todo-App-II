import React from "react";

import "./primary-button.css";

const PrimaryButton = ({ buttonName }) => {
  return <button className="primary-button">{buttonName}</button>;
};

export default PrimaryButton;
